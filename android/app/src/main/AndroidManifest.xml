<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.monobogdan.minisquadron">

    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:label="MiniSquadron">
        <activity android:screenOrientation="landscape" android:name="com.monobogdan.engine.android.MainActivity" android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
